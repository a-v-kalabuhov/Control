# <Термин RU> (`<КодСущности>`)

> Одна-две фразы: что это в терминах цеха/производства, зачем существует.

## Что это

Развёрнутое определение в доменных терминах. Кто с этим работает, какую роль играет
в производственном процессе. Без деталей реализации.

## Код

- Сущность/enum: `<Namespace.Class>`
- Связанные: `<другие сущности>`

## Ключевые поля

Только **доменный смысл** и **правила**, а не повтор типа из кода.

| Поле | Смысл и правила |
|---|---|
| `Field1` | Что означает в реальности; допустимые значения; что нельзя |
| `Field2` | … |

## Жизненный цикл / состояния

Если у сущности есть статусы/фазы — перечисли и опиши переходы (или сошлись на матрицу
в `docs/details/`). Если нет — удали раздел.

## Инварианты (нарушение = баг)

- Правило 1 — *почему* оно существует (одна фраза). → [CLAUDE.md] / [ADR-XXXX]
- Правило 2 — …

## Связанные

- ADR: [ADR-XXXX](../adr/XXXX-....md) — какое решение и почему
- Матрица/спека: `docs/details/...` или `docs/superpowers/specs/...`
- Сущности домена: [`other.md`](other.md)
